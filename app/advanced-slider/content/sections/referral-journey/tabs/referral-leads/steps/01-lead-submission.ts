import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "rj-2-1",
  content: "Referral Journey — Submit leads with minimal friction and see pipeline status in real time.",
};

export default slide;