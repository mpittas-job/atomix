import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "rj-1-1",
  content: "Referral Journey — Register introducers, agreements, and branding in a governed workflow.",
};

export default slide;