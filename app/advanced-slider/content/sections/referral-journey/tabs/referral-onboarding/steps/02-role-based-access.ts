import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "rj-1-2",
  content: "Referral Journey — Role-based access keeps referrer visibility appropriate at every stage.",
};

export default slide;