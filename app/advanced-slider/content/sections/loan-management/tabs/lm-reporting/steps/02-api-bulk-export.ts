import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "lm-3-2",
  content: "Loan Management — API and bulk export for warehouse lines and downstream systems.",
};

export default slide;