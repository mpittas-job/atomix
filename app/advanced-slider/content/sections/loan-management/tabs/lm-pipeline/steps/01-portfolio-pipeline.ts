import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "lm-1-1",
  content: "Loan Management — Portfolio-wide pipeline with SLA-based tasks and ownership.",
};

export default slide;