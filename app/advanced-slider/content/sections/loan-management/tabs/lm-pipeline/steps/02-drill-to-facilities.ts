import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "lm-1-2",
  content: "Loan Management — Drill from aggregate metrics to individual facilities in one click.",
};

export default slide;