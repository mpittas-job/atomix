import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "sj-documents-1",
  content:
    "Solicitor Journey — Collect, generate, and exchange documents with versioning, approvals, and a clear audit trail.",
};

export default slide;

