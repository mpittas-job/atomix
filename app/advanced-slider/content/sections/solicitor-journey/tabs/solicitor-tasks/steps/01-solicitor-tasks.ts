import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "sj-solicitor-tasks-1",
  content:
    "Solicitor Journey — Track assigned tasks, owners, due dates, and SLAs with lender-visible progress where applicable.",
};

export default slide;

