import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "sj-resolve-conflicts-1",
  content:
    "Solicitor Journey — Manage conflicts, exceptions, and enquiries with clear responsibility, messaging, and lender sign-off where required.",
};

export default slide;

