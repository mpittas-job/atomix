import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "sj-1-1",
  content: "Solicitor Journey — Receive lender instructions with matter references and conflict checks logged.",
};

export default slide;