import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "sj-2-2",
  content: "Solicitor Journey — Exceptions surfaced early so underwriting can react before completion.",
};

export default slide;