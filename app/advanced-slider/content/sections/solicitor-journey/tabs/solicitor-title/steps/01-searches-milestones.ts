import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "sj-2-1",
  content: "Solicitor Journey — Track searches, enquiries, and responses with lender-visible milestones.",
};

export default slide;