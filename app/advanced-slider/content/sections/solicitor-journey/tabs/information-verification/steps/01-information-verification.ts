import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "sj-information-verification-1",
  content:
    "Solicitor Journey — Verify borrower, property, and title information; flag discrepancies early and capture supporting evidence.",
};

export default slide;

