import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "sj-3-2",
  content: "Solicitor Journey — Post-close reporting closes the loop for audits and portfolio teams.",
};

export default slide;