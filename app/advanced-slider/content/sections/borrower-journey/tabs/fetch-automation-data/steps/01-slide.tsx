export default function FetchAutomationDataSlide01() {
  return (
    <p className="m-0">
      Fetch automation data — Dummy integration status panel. Displays mock
      progress bars for credit bureau pull, open banking aggregation, and
      land-registry lookup. Surface correlation ids and retry actions from your
      workflow engine in production.
    </p>
  );
}
