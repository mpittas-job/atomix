export default function GetStartedSlide02() {
  return (
    <p className="m-0">
      Get started (step 2) — Dummy next actions: verify phone with a fake OTP
      field, optional marketing preferences, and a disabled Continue until all
      required boxes are ticked. Replace with your auth provider callbacks and
      analytics events.
    </p>
  );
}
