export default function EligibilitySlide02() {
  return (
    <p className="m-0">
      Eligibility (step 2) — Dummy next steps when a check is review: upload
      supporting docs, book a call slot, or acknowledge a cooling-off notice.
      Map each branch to your workflow task types in production.
    </p>
  );
}
