import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "de-1-2",
  content: "Document Execution — Version control ensures only the latest pack can be released for signing.",
};

export default slide;