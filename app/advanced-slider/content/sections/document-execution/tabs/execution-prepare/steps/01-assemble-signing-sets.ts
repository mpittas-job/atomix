import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "de-1-1",
  content: "Document Execution — Assemble signing sets from approved templates with field validation.",
};

export default slide;