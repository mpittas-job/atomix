import type { Slide } from "@/app/advanced-slider/content/types";

const slide: Slide = {
  id: "de-2-2",
  content: "Document Execution — In-person or remote witnessing rules enforced per jurisdiction.",
};

export default slide;